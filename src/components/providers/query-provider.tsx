"use client";

import React from "react";

export function QueryProvider({ children }: { children: React.ReactNode }) {
  // Simplified provider for now - can be enhanced later with React Query
  return <>{children}</>;
}
